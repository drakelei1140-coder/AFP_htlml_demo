# 商户列表页 PRD 说明文档

## 1. 文档信息
- 文档名称：商户列表页 PRD 说明文档
- 适用页面：`merchant-list.html`
- 版本：v1.0
- 更新时间：2026-05-03
- 输出目的：沉淀商户审核列表页的页面结构、筛选口径、字段定义与交互规则，确保产品、设计、开发、测试执行一致。

---

## 2. 页面定位与目标

### 2.1 页面定位
商户列表页是「商户审核」主入口，用于按 MID/CID/SID 等维度检索商户审核单，并进入不同审核状态详情页执行后续动作。

### 2.2 页面目标
1. 支持多条件组合筛选，快速定位目标商户审核单。
2. 在列表直观展示商户审核状态、通道编码、审核人、更新时间等关键字段。
3. 支持跳转不同详情页，承接审核流。

### 2.3 目标用户
- 运营审核
- 风控审核
- 审批管理角色

---

## 3. 页面结构总览
1. 筛选区
2. 功能区
3. 列表区

---

## 4. 筛选区 PRD

### 4.1 字段清单（默认展示）
1. MID（输入）
2. 商户审核状态（下拉）
3. CID（输入）
4. SID（输入）
5. 渠道编码（多选）
6. 创建时间（区间输入）

### 4.2 字段清单（展开后附加）
7. 服务编码（多选）
8. 上單來源（多选）
9. 使用費率模板（输入）
10. 結算幣種（下拉）
11. 產品開通（多选）

### 4.3 关键枚举
#### 商户审核状态
- 待审核
- 基础资料审核
- 销售主管审核
- 风控核查
- 风控初级审核
- 风控中级审核
- 总经理审核
- 风控审核完成

### 4.4 按钮行为
- 查询：按条件刷新列表。
- 重置：恢复默认筛选。
- 展开/收起：切换附加筛选项显示。

---

## 5. 功能区 PRD
- 导出：导出当前筛选结果。
- 导入：导入商户数据（预留流程）。
- 新增：跳转 `merchant-add.html`。

---

## 6. 列表区 PRD

### 6.1 列字段（顺序）
1. MID
2. CID
3. 企业名称
4. SID
5. 商铺名称
6. 渠道编码
7. 服务编码
8. 进件通道
9. 商户审核状态
10. 当前审核人
11. 產品開通
12. 上單來源
13. 创建人
14. 创建时间
15. 更新时间
16. 操作

### 6.2 操作列
- 查看详情：根据审核状态跳转对应详情页（如 `merchant-detail-pending.html`、`merchant-detail-risk-*.html`、`merchant-detail-gm.html` 等）。

### 6.3 展示规范
- 审核状态使用 Badge。
- MID/CID/SID 支持复制按钮。
- 操作列固定右侧（sticky），保证横向滚动时可操作。

---

## 7. 交互规则
1. 支持筛选项展开/收起。
2. 列表支持横向滚动查看宽表字段。
3. 操作列始终可见。

---

## 8. 异常与空态
- 无数据：展示“暂无符合条件的数据”。
- 查询异常：提示错误并保留筛选条件。
- 字段缺失：显示 `-`。

---

## 9. 验收标准（UAT）
1. 筛选项与展开逻辑符合 PRD。
2. 列字段顺序与字段名一致。
3. 操作列固定右侧并可正常跳转详情页。
4. 审核状态可见且使用统一状态样式。
